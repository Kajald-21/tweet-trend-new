pipeline {
    agent {
        node {
            label 'maven'
            
        }
    }

environment{
    PATH = "/opt/apach-maven-3.9.12/bin:$PATH"
}
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install -DskipTests'
            }
        }
    }
}